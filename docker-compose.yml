version: '3.3'
services:
  my-vite-react-app:
    build: .
    ports:
      - "3000:3000"
    command: yarn dev
  nginx:
    image: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - my-vite-react-app